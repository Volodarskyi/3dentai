pipeline {
    agent {
        kubernetes {
            yaml """
            apiVersion: v1
            kind: Pod
            spec:
              containers:
              - name: jnlp
                image: docker:20.10.8
                securityContext:
                  privileged: true  # Allow Docker-in-Docker if necessary
                command:
                - cat  # Override default command to keep the container running
            """
        }
    }

    environment {
        DOCKER_HUB_TOKEN = credentials('dockerhub-token')
        DOCKER_IMAGE = 'oleksiipasichnyk/confl:hackaton-server'
        K8S_MANIFEST_PATH = 'server/kubernetes-configs/server-deployment-server.yaml'
        REPO_URL = 'git@github.com:Volodarskyi/3dentai.git'
        GIT_SSH_KEY = credentials('github-ssh-key')
        SERVER_DIR = 'server'
    }

    stages {
        stage('Install Git') {
            steps {
                // Install Git if it's not present
                sh 'apk add --no-cache git || apt-get update && apt-get install -y git'  // Adjust for Alpine or Debian base
            }
        }

        stage('Sparse Checkout Setup') {
            steps {
                sshagent(credentials: ['github-ssh-key']) {
                    script {
                        // Initialize Git repo and configure sparse checkout
                        sh """
                        git init
                        git remote remove origin || true
                        git remote add origin ${REPO_URL}
                        git config core.sparseCheckout true
                        echo '${SERVER_DIR}/*' > .git/info/sparse-checkout
                        git fetch --depth=1 origin main
                        git checkout main
                        """
                    }
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    def commitHash = sh(script: "git rev-parse --short HEAD", returnStdout: true).trim()
                    def date = sh(script: "date +%Y%m%d", returnStdout: true).trim()
                    def imageTag = "${DOCKER_IMAGE}-${commitHash}-${date}"

                    // Build Docker image within the 'server' directory
                    sh """
                    docker build -t ${imageTag} .
                    """
                }
            }
        }

        stage('Push Docker Image to DockerHub') {
            steps {
                script {
                    def commitHash = sh(script: "git rev-parse --short HEAD", returnStdout: true).trim()
                    def date = sh(script: "date +%Y%m%d", returnStdout: true).trim()
                    def imageTag = "${DOCKER_IMAGE}-${commitHash}-${date}"

                    // Authenticate with DockerHub and push the image
                    sh """
                    echo ${DOCKER_HUB_TOKEN} | docker login -u your-dockerhub-username --password-stdin
                    docker push ${imageTag}
                    """
                }
            }
        }

        stage('Update Kubernetes Manifest') {
            steps {
                dir("${SERVER_DIR}") {
                    script {
                        def commitHash = sh(script: "git rev-parse --short HEAD", returnStdout: true).trim()
                        def date = sh(script: "date +%Y%m%d", returnStdout: true).trim()
                        def imageTag = "${DOCKER_IMAGE}-${commitHash}-${date}"

                        // Use sed to replace only the image tag part of the manifest
                        sh """
                        sed -i 's|image: oleksiipasichnyk/confl:hackaton-server-.*|image: ${imageTag}|' ${K8S_MANIFEST_PATH}
                        """
                    }
                }
            }
        }

        stage('Rebase and Push to Deploy Branch') {
            steps {
                dir("${SERVER_DIR}") {
                    script {
                        sshagent(credentials: ['github-ssh-key']) {
                            // Rebase and push changes to the deploy branch
                            sh """
                            git checkout deploy || git checkout -b deploy
                            git rebase origin/main
                            git add ${K8S_MANIFEST_PATH}
                            git commit -m "Update image tag to ${DOCKER_IMAGE}-${commitHash}-${date}"
                            git push --force origin deploy
                            """
                        }
                    }
                }
            }
        }
    }
}
